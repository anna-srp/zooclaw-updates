---
title: "Chameleon 视频生成能力重大升级：支持超 15s 长视频 + 字幕擦除"
type: "产品基础功能更新"
priority: "高"
date: "2026-05-18"
status: "待审核"
channels: ""
---
# Chameleon 视频生成能力重大升级：支持超 15s 长视频 + 字幕擦除

## 核心宣传点
Chameleon 现在支持生成超过 15 秒的长视频（通过尾帧接龙技术），并新增一键字幕擦除能力，让视频素材更干净可用。

## 原始内容

**Commit**: `b2974359` | ecap-skills | 2026-05-18T07:58:59Z  
**PR**: #197 | feat(chameleon): seedance 链路对齐官方教程 + 接入字幕擦除 + 防呆校验

---

### 1. 尾帧接龙——突破 15s 单次生成上限

**业务价值** — 解决 Seedance 单次生成 ≤15s 的瓶颈：分段生成后用首帧锁住接缝，做出 >15s 的长视频。

- 单段跑完，API 同时返回视频最后一帧的图片 URL
- 下一段 CLI 直接 `--first-frame <那个 URL>` 续上，**省去 ffmpeg 抠帧的人工**
- ZooClaw 长视频业务（>15s 广告片、分镜接龙、内容工厂）的核心改造

### 2. 字幕擦除——一键清除 AI 生成的烧入字幕

**业务价值** — Seedance 偶尔会在画面里烧入字幕，用户想拿"干净画面"做二次创作或投放素材，可以一键调用 MediaKit 擦除。**已端到端实测通过**。

设计要点：
- **静默接入**：chameleon **永远不主动建议**字幕擦除，只在用户**显式**说出"擦字幕 / 去字幕 / erase subtitles"时路由
- **走 ecap-proxy**：不在客户端持有 ARK_API_KEY，安全

### 3. 客户端防呆参数校验

**业务价值** — 把"参数传错 → 上游报错 → 用户搞不清是 bug 还是配置"的循环砍掉，错误本地秒拒，还能省掉已经发生的 R2 上传带宽。

| 校验 | 规则 |
|---|---|
| duration | ∈ [4, 15] 或 -1（auto） |
| 参考资产数量 | image ≤9 / video ≤3 / audio ≤3 |
| Fast 模型 + 1080p | 拒绝（Fast 不支持 1080p） |
| 本地文件大小 | 图≤30MB / 视频≤50MB / 音频≤15MB |

### 4. 修复归档路径污染仓库的 bug

`DEFAULT_ARCHIVE_ROOT` 之前是相对路径会污染 repo 工作区，改成 `~/openclaw/chameleon/generations`。
